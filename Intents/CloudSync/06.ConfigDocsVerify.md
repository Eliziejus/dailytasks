# 6. ConfigDocsVerify

**Goal**: Document how to enable Firestore and apply security rules, correct the now-stale root `CONTEXT.md` constraints, and give the user a manual QA checklist — Claude cannot enable Firestore or apply rules in someone else's Firebase project.
**Est.**: 0.5h
**Dependencies**: FirestoreSetup, WireAppToFirestore, LocalStorageMigration, LoadingErrorStates

## Steps
- [x] Add a "Cloud sync (Firestore)" section to `README.md`: enable Firestore in the Firebase console (Build → Firestore Database → Create database), paste the security rules from intent 1 into Rules, no other setup needed since it's the same project as Auth
- [x] Update root `CONTEXT.md`'s Constraints section — remove the stale "no backend, no user accounts — localStorage only" line (already inaccurate since the Auth feature) and describe the current reality: Firebase Auth + Firestore, no server process, still a static deployment
- [x] Add a manual QA checklist: log in on two tabs/devices with the same account and confirm a task added in one appears in the other; confirm a browser with pre-existing `localStorage` tasks sees them migrated on first login; confirm logging in again doesn't duplicate them; confirm tasks persist across logout/login

## Definition of Done
- [x] A user with zero Firestore experience can follow the README and get cloud sync working
- [x] Root `CONTEXT.md` accurately reflects the app's actual architecture
- [x] The manual QA checklist covers every Definition of Done bullet from intents 3–4

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**: README gained a "Cloud sync (Firestore)" section (enable Firestore, publish `firestore.rules`, no extra config needed) and a 7-item manual QA checklist covering migration, real-time multi-tab sync, and the offline error path. The Usage section's stale "saved in `localStorage`" line (written during the Auth feature, before this one) is corrected to describe cloud sync. Root `CONTEXT.md` Constraints section rewritten — the "no backend, no user accounts, localStorage only" line was already stale as of the Auth feature and is now replaced with the actual architecture; also added `firebase-config.js` and `firestore.rules` to the Config files list, which had drifted out of date. Full end-to-end verification (actual sign-in, migration, real-time cross-tab sync) requires the user's live Firebase project with Firestore enabled and rules published — same limitation as the Auth feature. Final Playwright pass confirms all three pages still load cleanly with zero console errors against placeholder config.
- **Follow-ups**: User enables Firestore, publishes `firestore.rules`, and runs the manual QA checklist in README.md.
