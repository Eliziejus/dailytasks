# 3. WireAppToFirestore

**Goal**: Refactor `app.js`'s task CRUD from synchronous `localStorage` mutation to Firestore via `tasks-repo.js`, driven by a real-time subscription.
**Est.**: 1.5h
**Dependencies**: TaskRepository

## Steps
- [x] Convert `app.js` to an ES module (`<script type="module" src="js/app.js">` in `index.html`) so it can `import` from `tasks-repo.js` and `firebase.js`
- [x] Remove `loadTasks()` / `saveTasks()` (`localStorage`-based) and the `tasksByDate` variable's role as the write target; instead, subscribe once a user is confirmed (`onAuthStateChanged`) via `subscribeToTasks(uid, tasks => { tasksByDate = groupByDate(tasks); renderWeekStrip(); renderDayDetail(); })`
- [x] Add a `groupByDate(tasksArray)` helper that turns the flat array from Firestore back into the `{ [dateKey]: task[] }` shape the render functions already expect, so `renderWeekStrip`/`renderDayDetail`/`sortedTasks` need no changes
- [x] Replace the direct mutations in the checkbox-change handler, delete-button handler, and the add/edit form submit handler with calls to `updateTask` / `deleteTask` / `addTask` respectively — no local mutation or manual re-render needed since the `onSnapshot` subscription re-renders automatically
- [x] Unsubscribe the previous listener if one exists before starting a new one (defensive, in case auth state changes more than once in a session)

## Definition of Done
- [x] Adding, editing, completing, and deleting a task all go through `tasks-repo.js` — no direct Firestore or `localStorage` calls left in `app.js`
- [x] The UI updates when Firestore data changes, whether that change came from this tab or was pushed by the subscription
- [x] No regression in what intent 4 of DailyPlanner already verified (add/edit/cancel/validation), other than persistence now being Firestore instead of `localStorage`

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**: `app.js` is now an ES module, `index.html` updated accordingly. All `localStorage` code removed from `app.js` entirely (migration in intent 4 will read it directly in `tasks-repo.js` instead). Checkbox/delete/add/edit handlers now call the repo functions and add a `.catch` that logs the error and, for the checkbox, reverts the optimistic UI toggle on failure — `console.error` is a placeholder for intent 5's visible error banner. `onAuthStateChanged` in `app.js` runs independently of the one in `auth-guard.js`; on sign-out it tears down the subscription so a later sign-in as a different account doesn't merge two users' listeners. Cannot verify actual Firestore reads/writes without a real project (deferred to intent 6), but the page loads and redirects correctly with placeholder config (checked via the same Playwright script used for the Auth feature).
- **Follow-ups**: None beyond the deferred live-project verification.
