# 1. PageShell

**Goal**: Static HTML/CSS/JS skeleton with header, week-strip area, and day-detail area — no logic yet.
**Est.**: 0.5h
**Dependencies**: none

## Steps
- [x] Create `src/` with `index.html`, `css/styles.css`, `js/app.js`
- [x] HTML structure: header with app title, `<section id="week-strip">`, `<section id="day-detail">`
- [x] Link stylesheet and script from `index.html`
- [x] Minimal base CSS (layout, fonts, colors)

## Definition of Done
- [x] Opening `index.html` in a browser shows the empty shell with header, week-strip, and day-detail regions, basic styling applied
- [x] No console errors

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**: Verified via Playwright screenshot + console check (see conversation) — shell renders as expected, no errors.
- **Follow-ups**:
