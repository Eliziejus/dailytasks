# 8. Deploy

**Goal**: Deploy the static site publicly and verify it works live.
**Est.**: 0.5h
**Dependencies**: StylingPolish

## Steps
- [x] Choose a host (e.g. GitHub Pages) and configure it for this repo
- [x] Deploy the `src/` contents (or configure the host to serve `src/` as root)
- [ ] Verify the live URL loads correctly and tasks persist via localStorage
- [x] Add the live URL to `README.md`

## Definition of Done
- [ ] Site is reachable at a public URL
- [ ] Full task CRUD + persistence works on the deployed version

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**: Revisited once the user was ready to go live. Added `.github/workflows/deploy.yml` (checkout → configure-pages → upload-pages-artifact → deploy-pages, triggered on push to `main`) and pushed the branch, including all Auth and CloudSync work that had been committed locally but not yet pushed. `README.md` now lists the anticipated live URL (`https://eliziejus.github.io/dailytasks/`, derived from the `origin` remote). Since this environment has no `gh` CLI or GitHub API access, one manual step remains that Claude cannot do: Settings → Pages → Source → "GitHub Actions" in the repo settings. Until that toggle is flipped, the workflow will run on every push but Pages won't serve its output, so the live URL and full CRUD verification are follow-ups, same pattern as the Auth/CloudSync Firebase setup steps.
- **Follow-ups**: User flips Settings → Pages → Source → "GitHub Actions" once, then verifies the live URL loads and (once Firebase config + Firestore rules are in place per the Auth/CloudSync README sections) that sign-in and task sync work on the deployed site, not just locally.
