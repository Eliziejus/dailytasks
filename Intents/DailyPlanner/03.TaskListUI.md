# 3. TaskListUI

**Goal**: Render the selected day's tasks in `#day-detail` from in-memory sample data.
**Est.**: 1h
**Dependencies**: WeekCalendar

## Steps
- [x] Define an in-memory tasks data structure keyed by date (array of `{id, title, time, notes, done}`)
- [x] Render task list for the selected day, sorted by time
- [x] Show title, time, notes, and a done checkbox per task
- [x] Show an empty-state message when a day has no tasks

## Definition of Done
- [x] Selecting different days in the week strip updates the visible task list
- [x] Tasks display title/time/notes correctly, sorted by time

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**: Verified via Playwright — empty state shows with no tasks; injected sample tasks render sorted by time (timed tasks first, no-time last), done state shows strikethrough + checked box. No console errors. Checkbox/delete are not yet wired to actions (intent 5).
- **Follow-ups**:
