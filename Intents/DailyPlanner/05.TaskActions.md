# 5. TaskActions

**Goal**: Support marking a task complete/incomplete and deleting a task.
**Est.**: 1h
**Dependencies**: TaskForm

## Steps
- [x] Wire the done checkbox to toggle a task's done state and re-render (e.g. strikethrough styling)
- [x] Add a delete button/icon per task with a confirm step
- [x] Wire delete to remove the task from the day's data and re-render

## Definition of Done
- [x] Toggling the checkbox updates the task's done state visually and in data
- [x] Deleting a task removes it from the list immediately

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**: Verified via Playwright — checkbox toggles done state both ways (strikethrough applied/removed); delete uses `window.confirm`, dismiss keeps the task, accept removes exactly the targeted one. No console errors.
- **Follow-ups**:
