# 2. DocsVerify

**Goal**: Document the one Firebase console step required, and give the user a QA checklist — Claude cannot enable a sign-in provider in someone else's project.
**Est.**: 0.5h
**Dependencies**: WireGoogleSignIn

## Steps
- [x] Add to README's "Setting up auth" section: enable **Google** under Authentication → Sign-in method, set a support email (Firebase requires one for the Google provider)
- [x] Note that no additional Authorized domain step is needed — the domain list already added for email/password covers every provider
- [x] Add manual QA checklist items: Google button on both pages signs in a new Google account and lands on the planner; signing in with the same Google account again also works; closing the popup shows no error

## Definition of Done
- [x] A user with zero prior Google-provider experience can follow the README and get it working
- [x] Checklist covers every Definition of Done bullet from intent 1

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**: README's "Setting up auth" steps now include enabling the Google provider (inserted as step 3, before the web app config step so the ordering still matches what a user would click through top-to-bottom in the console), plus a note that the Authorized domains step already covers Google — nothing new to add there. Three checklist items added covering the two Definition of Done bullets from intent 1 (successful sign-in whether or not the account exists yet, and the silent-cancel path). Usage section's bullet list updated to mention Google as an option. Cannot be end-to-end verified without the user enabling the Google provider in their live project.
- **Follow-ups**: User enables Google under Authentication → Sign-in method (with a support email) and runs the manual QA checklist in README.md.
