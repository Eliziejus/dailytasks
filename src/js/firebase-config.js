// Firebase web config — get these values from:
// Firebase Console → Project Settings → General → Your apps → Web app → SDK setup and configuration
//
// These values are safe to commit: Firebase does not treat web config as secret.
// Access is controlled by Firebase Authentication settings and Security Rules, not by hiding this file.
// See README.md "Setting up auth" for step-by-step instructions.

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID",
};
